'use strict';

var React = require('react/addons');

require('../../../styles/Content/AddContent.less');

var AddContent = React.createClass({
  render: function () {
    return (
        <div>
          <p>Content for AddContent</p>
        </div>
      );
  }
});

module.exports = AddContent; 

