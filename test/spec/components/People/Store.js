'use strict';

describe('Store', function () {
  var React = require('react/addons');
  var Store, component;

  beforeEach(function () {
    //Store = require('../../../../src/scripts/components/People/Store.js');
    //component = React.createElement(Store);
  });

  it('should create a new instance of Store', function () {
     expect(true).toBeTruthy();
  });
});
