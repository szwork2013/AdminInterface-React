'use strict';

describe('Content', function () {
  var Store, component;

  beforeEach(function () {
    //Store = require('../../../../src/scripts/components/Content/Store.js');
    //component = React.createElement(Store);
  });

  it('should create a new instance of Store', function () {
     expect(true).toBeTruthy();
  });
});
